# Быстрый старт: Локальная разработка vs Продакшн

## 🚀 Локальная разработка

### 1. Создайте файл `web/.env.local`:
```env
VITE_API_URL=http://localhost:8000
```

### 2. Запустите бэкенд:
```bash
uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

### 3. Запустите фронтенд:
```bash
cd web
npm run dev
```

**Готово!** Приложение будет использовать локальный API на `http://localhost:8000`

---

## 🌐 Продакшн деплой

### Вариант 1: Через файл `.env.production`
Создайте `web/.env.production`:
```env
VITE_API_URL=https://padel-app-go6s.onrender.com
```

Затем соберите:
```bash
cd web
npm run build:prod
```

### Вариант 2: Через переменные окружения на сервере
Установите переменную `VITE_API_URL` на вашем хостинге (Netlify, Vercel и т.д.)

---

## ✨ Автоматический fallback

Если вы не создали `.env.local` или `.env.production`, приложение автоматически выберет:
- **В dev режиме** (`npm run dev`) → `http://localhost:8000`
- **В prod режиме** (`npm run build`) → `https://padel-app-go6s.onrender.com`

---

## 📝 Проверка текущего API URL

В консоли браузера (F12):
```javascript
console.log(import.meta.env.VITE_API_URL)
```

---

## 📚 Подробная документация

- **Локальная разработка:** `LOCAL_SETUP.md`
- **Настройка окружений:** `web/ENV_SETUP.md`
- **Деплой:** `DEPLOY.md`

